{
  "name": "normahl-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-thumbnails.mjs && astro build",
    "preview": "astro preview",
    "check": "astro sync",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "sharp": "^0.34.5"
  }
}
